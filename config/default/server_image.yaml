apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
  namespace: system
spec:
  template:
    spec:
      containers:
        # Change the value of image field below to your controller image URL
        - image: tink-server:latest
          name: tink-server
