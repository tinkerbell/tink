syntax = "proto3";
package workflow;

service Workflow {
	rpc CreateTemplate(CreateRequest) returns (Empty);
}

message Empty {
}

message CreateRequest {
    string template = 1;
    string target = 2;
}

